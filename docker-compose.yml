services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-mcp-agent-orchestrator:local
    environment:
      MCP_TRANSPORT: http
      HOST: 0.0.0.0
      PORT: 3000
      LOG_LEVEL: info
    ports:
      - "3000:3000"
    restart: unless-stopped
